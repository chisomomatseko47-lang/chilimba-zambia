'use client';
import { FormEvent, useEffect, useState } from 'react';
import { useRouter } from 'next/navigation';
import Link from 'next/link';
import { createClient } from '@/lib/supabase/client';
export default function ProfilePage(){const supabase=createClient();const router=useRouter();const [name,setName]=useState('');const [phone,setPhone]=useState('');const [saving,setSaving]=useState(false);const [message,setMessage]=useState('');useEffect(()=>{(async()=>{const {data:{user}}=await supabase.auth.getUser();if(!user){router.replace('/login');return}const {data}=await supabase.from('profiles').select('full_name,phone').eq('id',user.id).maybeSingle();if(data){setName(data.full_name||'');setPhone(data.phone||user.phone||'')}})()},[router,supabase]);async function save(e:FormEvent){e.preventDefault();setSaving(true);setMessage('');const {data:{user}}=await supabase.auth.getUser();if(!user){router.replace('/login');return}const {error}=await supabase.from('profiles').upsert({id:user.id,full_name:name.trim(),phone:phone.trim()});if(error)setMessage(error.message);else{setMessage('Profile saved securely.');setTimeout(()=>router.push('/dashboard'),500)}setSaving(false)}return <main className="shell"><nav className="topbar"><Link href="/dashboard">← Dashboard</Link><strong>Profile</strong></nav><section className="hero"><p className="eyebrow">ACCOUNT & SECURITY</p><h1>Your profile.<br/>Your identity.</h1><p>Keep your name and Zambian mobile number up to date for trusted group participation and payments.</p></section><section className="card form-card"><div><p className="eyebrow">PERSONAL DETAILS</p><h2>Account information</h2></div><label>Full name<input required value={name} onChange={e=>setName(e.target.value)} placeholder="e.g. Chisomo Maseko" /></label><label>Mobile number<input required type="tel" value={phone} onChange={e=>setPhone(e.target.value)} placeholder="+260 97 123 4567" /></label>{message&&<p className="notice" role="status">{message}</p>}<button className="btn" disabled={saving}>{saving?'Saving securely…':'Save profile'}</button></section><section className="card" style={{marginTop:18}}><p className="eyebrow">TRUST</p><h2>Built for Zambian savers</h2><p className="muted">Your profile connects your identity to your savings groups and mobile-money contribution records. Never share OTP codes with anyone.</p></section><nav className="mobile-nav"><Link href="/dashboard">⌂<span>Home</span></Link><Link href="/groups">♧<span>Groups</span></Link><Link href="/member">₭<span>Pay</span></Link><Link href="/transactions">◷<span>History</span></Link><Link className="active" href="/profile">♙<span>Profile</span></Link></nav></main>}
