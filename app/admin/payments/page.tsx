'use client';

import { useState } from 'react';

const providers=[['mtn','MTN MoMo'],['airtel','Airtel Money'],['zamtel','Zamtel Kwacha']];
export default function PaymentSettings(){const [provider,setProvider]=useState('sandbox');const [environment,setEnvironment]=useState('sandbox');return <main className="shell"><nav className="topbar"><a href="/dashboard">← Dashboard</a><strong>Payments</strong></nav><section><span className="eyebrow">PLATFORM SETTINGS</span><h1>Mobile money</h1><p className="muted">Configure the payment provider used by Chilimba. Credentials must stay in server-side environment variables.</p></section><section className="card form-card"><label>Default provider<select value={provider} onChange={e=>setProvider(e.target.value)}><option value="sandbox">Sandbox</option>{providers.map(([v,n])=><option value={v} key={v}>{n}</option>)}</select></label><label>Environment<select value={environment} onChange={e=>setEnvironment(e.target.value)}><option value="sandbox">Sandbox / test</option><option value="production">Production</option></select></label><div className="card"><strong>Selected: {provider==='sandbox'?'Sandbox':providers.find(x=>x[0]===provider)?.[1]}</strong><p className="muted">{provider==='sandbox'?'No real money moves. Use this while developing.':'Live credentials and provider-specific API implementation are required before activation.'}</p></div></section><section className="card"><h2>Required server configuration</h2><p className="muted">Provider secrets are never stored in the browser. Configure them through your deployment environment.</p><code>MTN_MOMO_API_KEY<br/>AIRTEL_MOMO_API_KEY<br/>ZAMTEL_MOMO_API_KEY<br/>PAYMENT_WEBHOOK_SECRET</code></section></main>}
