'use client';

import { useState } from 'react';

const providers=[['moneyunify','MoneyUnify — MTN, Airtel & Zamtel'],['sandbox','Sandbox']];
export default function PaymentSettings(){const [provider,setProvider]=useState('moneyunify');return <main className="shell"><nav className="topbar"><a href="/dashboard">← Dashboard</a><strong>Payments</strong></nav><section><span className="eyebrow">PLATFORM SETTINGS</span><h1>Mobile money</h1><p className="muted">Chilimba uses MoneyUnify as the Zambia mobile-money aggregator for MTN, Airtel and Zamtel. Keep credentials server-side.</p></section><section className="card form-card"><label>Default provider<select value={provider} onChange={e=>setProvider(e.target.value)}>{providers.map(([v,n])=><option value={v} key={v}>{n}</option>)}</select></label><div className="card"><strong>{provider==='moneyunify'?'MoneyUnify is selected':'Sandbox is selected'}</strong><p className="muted">{provider==='moneyunify'?'Collections use the MoneyUnify request-payment API. Transactions remain pending until verified; do not mark a contribution paid from the initial request alone.':'No real money moves in sandbox mode.'}</p></div></section><section className="card"><h2>Server configuration</h2><p className="muted">Add these in Vercel/server environment variables. Never put the MUID in client-side code.</p><code>MONEYUNIFY_MUID=your_business_muid<br/>PAYMENT_PROVIDER=moneyunify<br/>PAYMENT_WEBHOOK_SECRET=your_webhook_secret</code></section><section className="card"><h2>Supported wallets</h2><p>🇿🇲 MTN Mobile Money · Airtel Money · Zamtel Kwacha</p></section></main>}
